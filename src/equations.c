#include <math.h>
#include "../include/types.h"

int equestion2ndODE(const double t, const double x, const double p, double *result)
{
    *result = sin(t);
    return 0;
}

int motionEquationComponent(const double t, const double x, const double px, const pABS,
                            const double Ex, const double By, const double Bz,
                            double *result)
{
    *result =
}